meta {
  name: New Thread
  type: http
  seq: 4
}

post {
  url: http://localhost:5000/api/thread/new/{{user_id}}/{{board_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "thread_title":"api test thread 2",
    "op_body" : "body for API test thread"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
